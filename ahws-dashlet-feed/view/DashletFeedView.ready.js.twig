(() => {
    const oContent = document.querySelector('#{{ oUIBlock.GetId() }} .ahws-dashlet-feed-content');
    if (oContent && oContent.scrollHeight) {
        oContent.scrollTop = oContent.scrollHeight;
    }
})();
